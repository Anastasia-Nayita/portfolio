var x;
var doubleX;

x = 72;

function timesTwo(num) {
   num = num * 2;
    return (num);
};

timesTwo(x);
doubleX=timesTwo(x);
return (doubleX);

var numbers;

numbers [x, doubleX] 
function looping(numbers){
   // loop over
   (var i = 0; i <= numbers.length; i++);
   console.log(numbers[i]);
}
//overwrite number with empty obj
numbers = {
    y: doubleX;
};
